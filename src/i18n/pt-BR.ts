import en from "./en";

/**
 * Portuguese (Brazil) names. Falls back to English for unmapped entries.
 * Add or override keys as needed.
 */
const ptBR: Record<string, string> = { ...en, zapdos: "Zapdos" };

export default ptBR;
