import en from "./en";

/**
 * Portuguese (Brazil). Same as English for Pokémon names; override here only when pt-BR differs.
 */
const ptBR: Record<string, string> = { ...en };

export default ptBR;
